{
  "name": "stripr",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Camera-based LED pixel scanner and mapper",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node scripts/check.mjs",
    "bridge": "node bridge/server.mjs",
    "start": "node bridge/server.mjs --demo"
  },
  "engines": { "node": ">=22" },
  "license": "GPL-3.0-only"
}
